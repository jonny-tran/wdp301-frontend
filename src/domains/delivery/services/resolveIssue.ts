import { Issue } from '../models/Issue';

export const resolveIssue = async (issueId: string, resolution: string): Promise<Issue> => {
    // TODO: Implement API call
    throw new Error('Not implemented');
};
